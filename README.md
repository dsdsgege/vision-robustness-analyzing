# Vision Robustness Analyzing
Robustness testing of three different deep neural network.

## Project's Goal

- Implement three different deep neural networks in PyTorch
- Test the networks with different noises, such as Gaussian, Salt & Pepper, etc...
- Evaluate the models and visualize the data

## Prerequisites

Python 3

## Setup

1. Download the dependencies:
```bash
pip install torch torchvision matplotlib numpy
```

2. Run the model (WIP):

3. Run the evaluation script (WIP):
   
